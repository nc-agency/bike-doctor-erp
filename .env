# Umgebungsvariablen für die Docker-ERPNext-Installation für bike.doctor
# Erstellt am: 2025-05-05
# Zuletzt aktualisiert: 2025-05-05
# Anpassungen: 
# - Installation von ERPNext v15 mit Docker-Compose
# - Redis-Konfiguration korrigiert für Docker-Networking im URL-Format
# - PYTHONPATH angepasst für die Integration der bikedoctor-App
# - Optimierte Container-Ressourcen für bessere Performance
# - Anpassung an die offizielle frappe_docker-Konfiguration
# - Neue Umgebungsvariablen für verbesserte Stabilität

# Version configuration
FRAPPE_VERSION=version-15
ERPNEXT_VERSION=version-15
CUSTOM_IMAGE=frappe/erpnext
CUSTOM_TAG=version-15

# Site configuration
SITE_NAME=bikedoctor.localhost
SITES=bikedoctor.localhost
ADMIN_PASSWORD=admin
DB_ROOT_PASSWORD=frappe
DB_HOST=mariadb
DB_PORT=3306

# Redis configuration - muss URL-Format verwenden
REDIS_CACHE=redis://redis-cache:6379/0
REDIS_QUEUE=redis://redis-queue:6379/1
REDIS_SOCKETIO=redis://redis-socketio:6379/2
BENCH_LOCAL_REDIS=0

# Frappe user configuration
FRAPPE_USER=frappe

# Performance settings
WORKERS_COUNT=2
GUNICORN_WORKERS=4
NODE_OPTIONS="--max-old-space-size=1024"
MARIADB_INNODB_BUFFER_POOL_SIZE=512M

# Python path for custom apps
PYTHONPATH=/home/frappe/frappe-bench/apps

# Resource limits (optional - set to 0 for no limit)
BACKEND_CPUS_LIMIT=0
BACKEND_MEMORY_LIMIT=0
FRONTEND_CPUS_LIMIT=0
FRONTEND_MEMORY_LIMIT=0
SHORT_QUEUE_CPUS_LIMIT=0
SHORT_QUEUE_MEMORY_LIMIT=0

# Miscellaneous 
RESTART_POLICY=unless-stopped
PULL_POLICY=always
NO_MIGRATE=