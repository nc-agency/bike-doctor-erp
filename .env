# Umgebungsvariablen für die Docker-ERPNext-Installation für bike.doctor
# Erstellt am: 2025-05-05
# Anpassungen: 
# - Installation von ERPNext v15 mit Docker-Compose
# - Redis-Konfiguration korrigiert für Docker-Networking
# - PYTHONPATH angepasst für die Integration der bikedoctor-App
# - Optimierte Container-Ressourcen für bessere Performance

# Version configuration
ERPNEXT_VERSION=v15

# Site configuration
SITE_NAME=bikedoctor.localhost
SITES=bikedoctor.localhost
ADMIN_PASSWORD=admin
DB_PASSWORD=frappe

# Redis configuration
REDIS_CACHE_HOST=redis
REDIS_QUEUE_HOST=redis
REDIS_SOCKETIO_HOST=redis
BENCH_LOCAL_REDIS=0

# Frappe user configuration
FRAPPE_USER=frappe

# Performance settings
WORKERS_COUNT=2
GUNICORN_WORKERS=4
NODE_OPTIONS="--max-old-space-size=1024"
MARIADB_INNODB_BUFFER_POOL_SIZE=512M

# Python path for custom apps
PYTHONPATH=/home/frappe/frappe-bench/apps

# Miscellaneous 
RESTART_POLICY=unless-stopped
PULL_POLICY=always